# Face2Race Music

`GameAudio` loads menu and race music from this folder.

Expected loop clip names:

- `menu_loop`
- `race_loop`

Use WAV/OGG clips imported as `AudioClip` assets. They should already loop cleanly; the runtime `AudioSource` is set to loop.
